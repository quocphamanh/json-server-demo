{
  "name": "json-server-demo",
  "version": "1.0.0",
  "description": "how to use json server",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon main.js",
    "start": "node generate-data && main.js",
    "generate-data": "node generate-data.js"
  },
  "keywords": [
    "json-server"
  ],
  "author": "quocpa",
  "license": "MIT",
  "dependencies": {
    "faker": "^5.5.3",
    "json-server": "^0.17.0",
    "query-string": "^7.0.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
